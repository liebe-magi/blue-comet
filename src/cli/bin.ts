// CLI entry shim. Phase 3 fills this in with cac wiring.
// The placeholder simply prints a not-yet-implemented message so the bin
// is callable end-to-end during Phase 1/2 builds.

console.error('bluecomet: CLI is not yet implemented. See Phase 3.');
process.exit(1);
